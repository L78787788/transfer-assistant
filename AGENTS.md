# Project memory

## Notes

- 思考过程全部为中文
- 在每次执行完一个任务以后，清理此次任务产生的可以清除的缓存文件，以及其他无用的内容，防止存储垃圾内容过多。清除的缓存文件不要影响项目的推进。
- 安装的所有依赖、软件等所有东西都安装到 E 盘中去。如果必须安装到 C 盘，提前告知。
- 如果在项目推进的过程中安装了依赖或者其他东西，最后汇报的时候告知我，并告诉我安装在哪。
- 每次开始做一个项目（或新建一个项目）时，要为该项目制作一个快捷启动方式，存放到固定路径 `D:\草稿箱\reasonix`，方便一键启动测试。
  - 路径固定为 `D:\草稿箱\reasonix`，文件名用项目名命名（如 `grok-build.bat`），已有同名文件时覆盖更新。
  - 快捷方式的形式由 AI 根据项目类型选择最合适的：
    - 命令行/脚本项目（Node、Python、Go、Rust 等）：生成 `.bat`/`.cmd`，内容为 `cd /d 项目路径` + 启动命令，并 `pause` 或保持窗口以便查看日志；
    - 需要打开浏览器的 Web 项目：启动脚本里同时打开对应地址（如 `start http://localhost:3000`）；
    - 桌面应用等场景：用 PowerShell 创建 `.lnk` 快捷方式；
    - 纯网页地址类：`.url` 文件。
  - 项目启动命令发生变化（如端口、脚本名改动）时，同步更新对应的快捷启动文件。
  - 汇报任务时如涉及创建/更新快捷方式，简要告知用户文件位置和启动方式。
- 保存记忆时必须双写两份，两边才会同时生效：
  - `E:\zcode save\.zcode\cli\memories\projects\...` ← 客户端界面显示（由环境变量 `ZCODE_DATA_BASE_DIR=E:\zcode save` 决定）
  - `C:\Users\19905\.zcode\cli\memories\projects\...` ← CLI 会话注入
  - 两个位置的文件和 MEMORY.md 索引都要同步更新。
