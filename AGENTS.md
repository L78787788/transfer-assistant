# Project memory

## Notes

- 思考过程全部为中文
- 在每次执行完一个任务以后，清理此次任务产生的可以清除的缓存文件，以及其他无用的内容，防止存储垃圾内容过多。清除的缓存文件不要影响项目的推进。
- 安装的所有依赖、软件等所有东西都安装到 E 盘中去。如果必须安装到 C 盘，提前告知。
- 如果在项目推进的过程中安装了依赖或者其他东西，最后汇报的时候告知我，并告诉我安装在哪。
- 每次开始做一个项目（或新建一个项目）时，要在固定路径 `D:\草稿箱\Gemini` 下创建一个以项目名命名的文件夹（如 `D:\草稿箱\Gemini\传输助手`），在里面放置该项目的启动方式，方便一键启动测试。
  - 如果有启动程序（如 .exe 或已编译的桌面应用），在文件夹内创建该启动程序的快捷方式（即 Windows 系统右键菜单中可创建的 `.lnk` 快捷方式）；
  - 如果没有启动程序（如脚本、Node、Python、Web 项目等），在文件夹内写一个启动脚本（依然以项目名命名，如 `<项目名>.bat` 或 `<项目名>.cmd`），内容为 `cd /d 项目路径` + 启动命令，并 `pause` 或保持窗口以便查看日志，Web 项目可同时打开浏览器；
  - 项目启动命令或程序路径发生变化时，同步更新对应的快捷启动文件；
  - 汇报任务时如涉及创建/更新快捷方式，简要告知用户文件位置和启动方式。
- 保存记忆时必须双写两份，两边才会同时生效：
  - `E:\zcode save\.zcode\cli\memories\projects\...` ← 客户端界面显示（由环境变量 `ZCODE_DATA_BASE_DIR=E:\zcode save` 决定）
  - `C:\Users\19905\.zcode\cli\memories\projects\...` ← CLI 会话注入
  - 两个位置的文件和 MEMORY.md 索引都要同步更新。
